# ===== Admin Panel =====
# Credenziali per accedere al pannello admin (/admin)
ADMIN_USER=admin
ADMIN_PASSWORD=cambia-questa-password

# Secret per firmare i JWT (genera con: openssl rand -hex 32)
ADMIN_JWT_SECRET=genera-un-secret-random-lungo

# ===== Vercel Blob Storage =====
# Token per Vercel Blob (lo trovi in Vercel Dashboard > Storage > Blob)
# Senza questo token, il sito usa i dati locali da projects.json
BLOB_READ_WRITE_TOKEN=vercel_blob_xxxxx

# ===== Notion (opzionale, non in uso) =====
# NOTION_API_KEY=secret_xxxxx
# NOTION_DATABASE_ID=xxxxx
